@import 'variables.styl';

.company-card {
  background:#fff;
  box-shadow: 0 1px 10px rgba(0,0,0,0.20), 0 0 0 1px rgba(0,0,0,.04);
  border-radius: 2px;
  width 100%
  max-width 600px
  margin 40px auto
}